{
  "name": "just-youtrack-helpdesk-ticket-2-issue-sync",
  "description": "Auto-create and sync linked dev issues from helpdesk tickets",
  "version": "0.1.0",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "bun run scripts/build.ts",
    "upload": "bun run scripts/build.ts && bun run scripts/upload.ts",
    "prezip": "rm -rf helpdesk-sync.zip",
    "zip": "bun run scripts/build.ts --zip"
  },
  "devDependencies": {
    "@jetbrains/youtrack-scripting-api": "latest",
    "@types/archiver": "^7.0.0",
    "@types/bun": "latest",
    "archiver": "^7.0.1",
    "youtrack-workflow-api-types": "^0.2.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "author": {
    "name": "Artur Kozubov",
    "email": "arturkozubov@uteg.sk"
  }
}
